name: shinsedaikan_dojo
description: Shinsedaikan Dojo Management (Flutter web)
publish_to: 'none'

environment:
  sdk: '>=2.17.0 <3.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.15
  flutter_svg: ^2.0.7
  qr_flutter: ^4.0.0
  pdf: ^3.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.jpg
    - assets/avatars/
